# =============================================================================
# Weft Docker Environment Variables Template
# =============================================================================
# Copy this file to .env and customize for your environment.
#   cp docker/.env.example .env
#
# SECURITY WARNING: Never commit .env to version control!
# =============================================================================

# ---------------------------------------------------------------------------
# Application Environment
# ---------------------------------------------------------------------------
NODE_ENV=development

# ---------------------------------------------------------------------------
# PostgreSQL Database Configuration
# ---------------------------------------------------------------------------
POSTGRES_USER=weft
POSTGRES_PASSWORD=weft_dev_password
POSTGRES_DB=weft
POSTGRES_PORT=5432

# Database URL (auto-generated from above, customize if needed)
# DATABASE_URL=postgresql://weft:weft_dev_password@localhost:5432/weft

# ---------------------------------------------------------------------------
# Redis Configuration
# ---------------------------------------------------------------------------
REDIS_PORT=6379

# Redis URL (auto-generated, customize if needed)
# REDIS_URL=redis://localhost:6379

# ---------------------------------------------------------------------------
# Backend Service Configuration
# ---------------------------------------------------------------------------
BACKEND_PORT=4000

# Better Auth Configuration
BETTER_AUTH_URL=http://localhost:4000
BETTER_AUTH_SECRET=development-secret-key-change-in-production-min-32-chars

# Frontend URL (used for CORS and redirects)
# Set this to the URL where users access the frontend
FRONTEND_URL=http://localhost:3000

# Additional CORS origins (comma-separated, optional)
# Use this if your frontend is accessed from multiple URLs
# Example: CORS_ORIGINS=https://app.example.com,https://www.example.com
# CORS_ORIGINS=

# ---------------------------------------------------------------------------
# Frontend Service Configuration
# ---------------------------------------------------------------------------
FRONTEND_PORT=3000

# Backend API URL for frontend (used at build time by Vite)
# This is the URL the browser uses to reach the backend API
# - For local development with port forwarding: http://localhost:3001
# - For production deployment: https://api.yourdomain.com
VITE_API_URL=http://localhost:3001

# ---------------------------------------------------------------------------
# Optional: External Services (uncomment if needed)
# ---------------------------------------------------------------------------
# OpenAI API for AI features
# OPENAI_API_KEY=sk-...

# Video processing storage path
# VIDEO_STORAGE_PATH=/var/lib/weft/videos

# Log level (debug, info, warn, error)
# LOG_LEVEL=info
